/*  1. npx prisma migrate dev/prod 
    2. generating the prisma client  
*/   


generator client {
  provider = "prisma-client-js"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
